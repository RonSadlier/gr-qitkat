Ensure that you've installed GNU Radio and gr-qitkat should be ready to build. We only need the dependencies that GNU Radio requires.

Building from cmake:

$ mkdir build
$ cd build
$ cmake ../
$ make
$ make test
$ sudo make install
